{% extends 'base.html.twig' %}

{% block title %}Member list{% endblock %}

{% block body %}
    <ul>
        {% for member in members %}
            <li>{{ member.id }} <a href="{{ url('details', {memberId: member.id}) }}"> {{ member.email }}  </a> {{ member.firstName }} {{ member.lastName }}</li>
        {% endfor %}
    </ul>

    <div {{ stimulus_controller('hello') }}>
        ...
    </div>
{% endblock %}